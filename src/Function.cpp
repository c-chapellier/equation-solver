
#include "Function.hpp"

Function::Function(std::string name, std::vector<std::string> &args_names, System *sys, Exp *exp) : name(name), args_names(args_names), sys(sys), exp(exp)
{
    
    
    
    
}

